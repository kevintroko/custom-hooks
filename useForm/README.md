# Use form
Example:
```
    const initialForm = {
        name: 'Kevin',
        age: 26,
        email: 'kevin@gmail.com',
    }
    const [formValues, handleInputChange, reset] = useForm(initialForm);
```
